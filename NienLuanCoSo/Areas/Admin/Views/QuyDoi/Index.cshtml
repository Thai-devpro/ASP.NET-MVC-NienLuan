@model IEnumerable<NienLuanCoSo.TT_QUYDOI>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Danh sách quy đổi</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        
        
        <th>
            Mã quyên góp hiện kim
        </th>
        <th>
           Số lượng
        </th>
        <th>
           Trị giá hiện kim
        </th>
        <th>
         Ngày quy đổi
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        
        
        <td>
            @Html.DisplayFor(modelItem => item.TT_QUYENGOP_HIENKIM.SOTIEN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SOLUONG)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TRIGIA_QD)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NGAY_QD)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
